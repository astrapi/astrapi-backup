//-     if _scheduler.running:
        return 
  in init_scheduler einbauen
//- print("Registered routes:") aus app/api/app.py entfernen


//- falscher Text beim Aktivieren eines Jobs
//- Alle aktivieren / deaktivieren einbauen
- warum kann node bei proxmox_lxc nich als fqn angegeben werden?
//- run all und run all (debug) fehlen
- auch im datk mode monochrome icons
- wochentage nach wie vor nicht sichtbar im scheduler (light mode)
//- run-badge wird nicht aktualisiert
//- in der Anzeige:   SRC -> Quelle
                    DST -> Ziel
                    Type -> Typ

ggf. den Menüpunkt in Einstellugen ändern und folgendes dort verfügbar machen, ich brauche nicht mehrere scheduler jobs, es reicht wenn eingestellt werden kann wann das backup laufen soll, und wenn es läuft dann immer alles

- ntfy Konfiguration hibzufügen
- scheduler auf db umstellen
- alles unter app/config/secrets.env sicher speichern



v60
- run-badge wird immer noch nicht aktualisiert
- bei run all leider kein run-badge
- höhe von "Neu" an "Alle an" anpassen
- auch im dark mode monochrome icons
- ntfy-Benachrichtigungen: nur URL und THEMA abfragen
- "Wake-on-LAN (backup02)" ändern in "Wake on LAN" -> mehrere MAC-Adressen + Host zulassen
- BORG_PASSPHRASE, PBS_PASSWORD und PBS_FINGERPRINT fehlen
- "Aktiv" bei Backup-Zeitplan in den Titel der Karte, wie bei den Jobs

v61
- design an gnome anpassen
- als app installierbar (chrome)
- ntfy test